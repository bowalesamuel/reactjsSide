import React from "react";
import ModalWrapper from "../Modals";
import styles from "./styles.module.scss";

const Deposit = ({ data, schema, renderRight }) => {
  return (
    <ModalWrapper>
      <h2 className={styles.deposit__title}>

      </h2>
    </ModalWrapper>
  );
};

export default Deposit;
